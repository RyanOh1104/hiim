[uwsgi]
chdir = /srv/hiim/
module = mvp.wsgi:application
home = /home/ubuntu/myvenv/

uid = hiim-deploy
gid = hiim-deploy

http = :8080

enable-threads = true
master = true
vacuum = true
pidfile = /tmp/mvp.pid
logto = /var/log/uwsgi/mvp/@(exec://date +%%Y-%%m-%%d).log
log-reopen = true